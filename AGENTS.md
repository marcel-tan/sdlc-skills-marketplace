# SDLC skills marketplace

Before starting a task, identify its SDLC stage and invoke the matching skill
(`/sdlc-<stage>:<skill>`): specs → stories → codegen → testing → pr-review →
code-hygiene. Each skill ends with a hand-off naming the next skill to run.
If no skill fits, proceed normally.
